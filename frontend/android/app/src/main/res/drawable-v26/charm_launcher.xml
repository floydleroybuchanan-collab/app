<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/charm_icon_background" />
    <foreground>
        <!-- Foreground PNG already includes the adaptive-icon safe-area padding. -->
        <bitmap android:src="@drawable/charm_refined_foreground" android:gravity="fill" android:filter="true" />
    </foreground>
</adaptive-icon>
